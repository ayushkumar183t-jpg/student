Task 1 (Dictionary Lookup): Creates a name-to-mark database. Checks if a user's input name exists: prints the score if found, or displays "Student not found." if missing.

Task 2 (List Slicing): Generates numbers 1 to 10. Uses [:5] to grab the first five elements [1, 2, 3, 4, 5], and uses [::-1] to reverse them into [5, 4, 3, 2, 1]
